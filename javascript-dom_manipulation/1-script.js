const clickableItem = document.querySelector('#read_header');

clickableItem.addEventListener('click', function()
{
    const header = document.querySelector('header');
    header.getElementsByClassName.color = '#FF000';
});